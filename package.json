{
  "name": "longevity-proposal",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "pretest": "npx playwright install chromium",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.1"
  }
}
